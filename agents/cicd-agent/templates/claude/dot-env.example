# Generated by @human.tech/create-agent-wallet
# Activity: Wallet Integration Test Agent (Claude runtime)

# Required — GitHub personal access token (needs repo + actions scopes)
GITHUB_TOKEN=

# Required — Comma-separated repos to monitor (e.g. owner/repo1,owner/repo2)
GITHUB_REPOS=

# Required — Staging/preview environment base URL to run wallet tests against
STAGING_URL=

# Required — Contract address for contract interaction tests
TEST_CONTRACT_ADDRESS=

# Required — ERC-20 token address for approval tests
TEST_TOKEN_ADDRESS=

# Optional — Polling interval in milliseconds
# POLL_INTERVAL_MS=30000

# Optional — Webhook URL for deploy-on-merge (e.g. Vercel deploy hook)
# DEPLOY_WEBHOOK_URL=

# Optional — GitHub Actions workflow file to dispatch for CI
# CI_WORKFLOW_ID=ci.yml

# Optional — ETH amount for the transaction test (sent to self on testnet)
# TEST_SEND_AMOUNT_ETH=0.0001

# Optional — Hex-encoded calldata for contract interaction test
# TEST_CONTRACT_METHOD=0x
