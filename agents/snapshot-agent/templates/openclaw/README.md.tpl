# {{projectName}} (OpenClaw runtime)

AgentSkills-compliant skill. OpenClaw loads from
`~/.openclaw/workspace/skills/<name>/SKILL.md`.

## Install

```bash
cp -r ./ ~/.openclaw/workspace/skills/{{projectName}}
cp .env.example .env   # set SNAPSHOT_SPACES
```

## Recipe

{{recipeUrl}}
