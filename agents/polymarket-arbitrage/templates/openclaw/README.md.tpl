# {{projectName}} (OpenClaw runtime)

AgentSkills-compliant arbitrage skill. OpenClaw loads from
`~/.openclaw/workspace/skills/<name>/SKILL.md`.

## Install

```bash
cp -r ./ ~/.openclaw/workspace/skills/{{projectName}}
cp .env.example .env   # set AGENT_MAX_ORDER_USD
```

## Recipe

{{recipeUrl}}
