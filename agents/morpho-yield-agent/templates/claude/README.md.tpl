# {{projectName}} (Claude runtime)

Morpho yield skill for Claude Code.

## Files

| File | Purpose |
|---|---|
| `SKILL.md` | Skill definition |
| `CLAUDE.md` | Project context |
| `mcp-config.json` | WaaP MCP wiring |
| `.env.example` | Required env vars |

## Use it

1. `cp .env.example .env` and set `AGENT_ASSET` + `AGENT_MAX_DEPOSIT_USD`.
2. Open in Claude Code.
3. Ask Claude to find the best vault and deposit.

## Recipe

{{recipeUrl}}
