# {{projectName}} (OpenClaw runtime)

AgentSkills-compliant skill.

## Install

```bash
cp -r ./ ~/.openclaw/workspace/skills/{{projectName}}
cp .env.example .env   # set AGENT_ASSET + AGENT_MAX_DEPOSIT_USD
```

## Recipe

{{recipeUrl}}
