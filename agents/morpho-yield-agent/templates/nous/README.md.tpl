# {{projectName}} (Hermes Agent / Nous runtime)

Morpho Blue yield agent for Hermes.

## Run

Install Hermes, `cp .env.example .env`, set `AGENT_ASSET` + `AGENT_MAX_DEPOSIT_USD`, register as a Hermes skill.

## Recipe

{{recipeUrl}}
