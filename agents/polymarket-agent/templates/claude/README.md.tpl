# {{projectName}} (Claude runtime)

Polymarket agent skill for Claude Code.

## Files

| File | Purpose |
|---|---|
| `SKILL.md` | Skill definition |
| `CLAUDE.md` | Project context |
| `mcp-config.json` | WaaP MCP server config |
| `.env.example` | Required vars (`AGENT_MAX_ORDER_USD` is required) |

## Use it

1. `cp .env.example .env` and set `AGENT_MAX_ORDER_USD`.
2. Open this folder in Claude Code.
3. Ask Claude to scan markets or place a test order within your cap.

## Recipe

{{recipeUrl}}
