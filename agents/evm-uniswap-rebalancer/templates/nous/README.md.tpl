# {{projectName}} (Hermes Agent / Nous runtime)

Uniswap v3 LP rebalancer for Hermes Agent.

## Run

Install Hermes, `cp .env.example .env`, set `AGENT_POSITION_ID` + `AGENT_MAX_DEPOSIT_USD`, then register this folder as a Hermes skill.

## Recipe

{{recipeUrl}}
