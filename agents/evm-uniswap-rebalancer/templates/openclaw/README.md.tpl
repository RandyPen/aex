# {{projectName}} (OpenClaw runtime)

AgentSkills-compliant Uniswap v3 LP rebalancer.

## Install

```bash
cp -r ./ ~/.openclaw/workspace/skills/{{projectName}}
cp .env.example .env   # set AGENT_POSITION_ID + AGENT_MAX_DEPOSIT_USD
```

## Recipe

{{recipeUrl}}
